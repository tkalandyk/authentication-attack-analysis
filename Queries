# 🔍 Investigation Queries – Authentication Attack Analysis

This document contains the exact KQL queries used during the investigation, along with their purpose and observed results.

---

## 🧩 Query 1 — Failed Authentication Attempts

**Purpose:**  
Identify brute-force or password-guessing activity against the endpoint.

**Data Source:** `DeviceLogonEvents`

```kql
DeviceLogonEvents
| where ActionType == "LogonFailed"
| where DeviceName contains "js-mde-test"
| summarize FailedLogons = count() by DeviceName

🧩 Query 2 — Successful Authentication Attempts

Purpose:
Confirm whether failed authentication attempts eventually resulted in successful access.
